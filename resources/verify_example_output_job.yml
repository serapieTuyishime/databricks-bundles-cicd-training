resources:
  jobs:
    verify_example_output_job:
      name: verify-example-output-${bundle.target}

      tasks:
        - task_key: verify
          notebook_task:
            notebook_path: ../src/verify_example_output.ipynb
#          job_cluster_key: serverless_cluster

      job_clusters:
#        - job_cluster_key: serverless_cluster
          new_cluster:
            spark_version: 14.3.x-scala2.12
            num_workers: 1
            node_type_id: Standard_DS3_v2
